= render 'h2', locals: { name: 'services' }
.restricted-width
  .content
    = render 'services'
%br(clear='all')

= render 'h2', locals: { name: 'resume' }
.restricted-width
  .content
    = render 'resume'
%br(clear='all')

= render 'h2', locals: { name: 'contact' }
.restricted-width
  .content
    = render 'contact'
%br(clear='all')
